import { CellDayFilterPipe } from "./cell-day-filter.pipe";


describe('CellDayFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CellDayFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
