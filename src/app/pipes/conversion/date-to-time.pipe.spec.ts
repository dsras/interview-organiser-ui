import { DateToTimePipe } from './date-to-time.pipe';

describe('DateToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DateToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
